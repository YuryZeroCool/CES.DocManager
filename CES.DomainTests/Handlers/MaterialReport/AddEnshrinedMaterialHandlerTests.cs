using AutoMapper;
using CES.Domain.Models.Response.MaterialReport;
using CES.Infra;
using CES.Infra.Models;
using CES.Infra.Models.MaterialReport;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Moq;
using Moq.EntityFrameworkCore;
using Assert = Microsoft.VisualStudio.TestTools.UnitTesting.Assert;

namespace CES.Domain.Handlers.MaterialReport.Tests
{
    [TestClass()]
    public class AddEnshrinedMaterialHandlerTests
    {
        [TestMethod()]
        public void AddEnshrinedMaterialHandlerTest()
        {
            Assert.Fail();
        }

        [TestMethod()]
       public void HandleTest()
        {
        //    var userContextMock = new Mock<DocMangerContext>();
        //    var userMapperMock = new Mock<IMapper>();
        //   var epm = new EmployeeEntity() { FirstName= "Воднев" };

        //    var enshrine = new EnshrinedMaterialEntity();
            
         

        //   userContextMock.Setup(x => x.Parties).ReturnsDbSet(employees);
        //    //userMapperMock.Setup<IMapper>(p => p.Ma;
        ////  var sss=  d.toList();
        //    var usersService = new AddEnshrinedMaterialHandler(userContextMock.Object,userMapperMock.Object);
        //    Console.WriteLine("ss");
        //    Assert.Equals("Воднев", employees.FirstOrDefault(x => x.FirstName == "Воднев").FirstName);
        }
    }
}