// import { createAsyncThunk, createSlice } from "@reduxjs/toolkit";


export {}

// export const VehicleExpenseSheet = createAsyncThunk(
//     "jws",
//     async (event : Event, { rejectWithValue }) => {

//         const formData = new FormData(event.target.from);
//       return await axios
//         .post<IAuthResonse>("https://localhost:5001/login", user)
//         .then((response) => {
//           return response.data;
//         })
//         .catch((error) => {
//           console.log(error.message);
//           return rejectWithValue(error.message);
//         });
//     }
// );


//     const vehicleExpenseSheetSlice = createSlice({
//         name: "auth",
//         initialState: initial,
//         reducers: {},
//         extraReducers: (builder) => {
//           builder.addCase(login.pending, (state) => {
//             state.status = "loading";
//             state.error = "";
//           });
        
//           })
       
      
      
//   );